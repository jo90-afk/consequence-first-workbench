interface:
  display_name: Consequence-First Design
  short_description: Trace decisions through downstream effects
  default_prompt: Use $consequence-first-design to trace this change through the whole
    system.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  allow_implicit_invocation: true
